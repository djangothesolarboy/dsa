/* 
    linear time: o(n)
*/

const printElementsOfArray = function (arr) {
    for (let i = 0; i < arr.length; i++) {
        console.log(`${arr[i]}`);
    };
};

printElementsOfArray(['e', 'f', 'sdf']);